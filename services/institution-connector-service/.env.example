# Credenciais Pluggy (dev local). Copie para .env e preencha. NÃO commitar o .env.
# Em produção, use o AWS Secrets Manager (ArquiteturaOpenSight.md §4.3).
PLUGGY_CLIENT_ID=
PLUGGY_CLIENT_SECRET=
# Opcional: item (conexão) para o smoke do connector-service listar contas.
PLUGGY_ITEM_ID=
