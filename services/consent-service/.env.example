# consent-service (dev). Copie para .env. NÃO commitar o .env.
PORT=8087
CORS_ORIGINS=http://localhost:5173,http://localhost:5174
# AUTH_PUBLIC_KEY_FILE=./auth_public.pem
JWT_ISSUER=opensight-auth
# Postgres (opcional): se definido, usa o banco; senão, repositório em memória (dev).
# DATABASE_URL=postgres://user:pass@localhost:5432/consent_db?sslmode=require
