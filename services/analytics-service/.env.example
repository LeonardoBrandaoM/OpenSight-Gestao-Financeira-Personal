# analytics-service (dev local). Copie para .env. NÃO commitar o .env.
PORT=8084
CORS_ORIGINS=http://localhost:5173,http://localhost:5174
# AUTH_PUBLIC_KEY_FILE=./auth_public.pem   # exige JWT quando setado
JWT_ISSUER=opensight-auth
# Postgres (opcional): se definido, usa o banco; senão, repositório em memória (dev).
# DATABASE_URL=postgres://user:pass@localhost:5432/analytics_db?sslmode=require
