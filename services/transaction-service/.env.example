# transaction-service (dev local). Copie para .env. NÃO commitar o .env.
PORT=8083
# DATABASE_URL=postgres://tx_app:senha@localhost:5432/transaction_db?sslmode=require
CORS_ORIGINS=http://localhost:5173,http://localhost:5174
# AUTH_PUBLIC_KEY_FILE=./auth_public.pem   # exige JWT quando setado
JWT_ISSUER=opensight-auth
